
<% if user_signed_in? %>
<h1>Friends</h1>
<div class="table-responsive">
<div id="friends">
  
  <table class="table table-striped table-bordered table-hover"><strong>
<thead class="table-dark">
  <tr>
   <th>Name</th>
  
  <th>Email</th>
  <th>Phone</th>
  <th>Twitter</th>
  <th>User Id</th>
 
</tr>
</thead>

<tbody>

  <% @friends.each do |friend| %>
  <% if friend.user == current_user %>
   <tr>
      <!--<div id="<%= dom_id friend %>">-->
        <td> <%= link_to friend.first_name + ' '+friend.last_name, friend, class:"text-decoration-none" %>
     </td>
      <td> <%= friend.email %></td>
      <td><%= friend.phone %></td>
      <td> <%= friend.twitter %></td>
      <td><%=friend.user_id %></td>
   
  </tr>
  <% end %>
 <% end %>
</tbody>
</strong>
</table>

</div>
</div>
<%= link_to "New friend", new_friend_path,class:"btn btn-secondary" %>
<% else %>
<h1>Hello World</h1>
This is my first webpage in ruby......

<% end %>